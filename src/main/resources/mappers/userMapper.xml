<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.traffic.practice.mapper.UserProfileMapper">

    <select id="getUserProfile" resultType="com.traffic.practice.dto.UserDTO">
        SELECT id, user_id, password, nickname, created_time, is_with_draw, status
        FROM user
        WHERE id = #{id}
    </select>

    <insert id="insertUserProfile" parameterType="com.traffic.practice.dto.UserDTO">
        INSERT INTO user (user_id, password, nickname, is_with_draw, status, is_admin)
        VALUES (#{userId}, #{password}, #{nickName}, #{isWithDraw}, #{status}, #{isAdmin})
    </insert>

    <update id="updateUserProfile" parameterType="com.traffic.practice.dto.UserDTO">
        UPDATE user
        SET password=#{password},
            nickname=#{nickName},
            is_with_draw=#{isWithDraw},
            status=#{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteUserProfile">
        DELETE
        FROM user
        WHERE user_id = #{id}
    </delete>

    <insert id="register" parameterType="com.traffic.practice.dto.UserDTO">
        INSERT INTO user (user_id, password, nickname, created_time, is_with_draw, status)
        VALUES (#{userId}, #{password}, #{nickname}, #{createdTime}, #{isWithDraw}, #{status})
    </insert>

    <select id="findByIdAndPassword" resultType="com.traffic.practice.dto.UserDTO">
        SELECT id,
               user_id,
               password,
               nickname,
               created_time,
               is_with_draw,
               status
        FROM user
        WHERE user_id = #{userId}
          AND password = #{password}
          AND status != 'DELETE'
    </select>

    <select id="idCheck" resultType="int">
        SELECT COUNT(id)
        FROM user
        WHERE user_id = #{userId}
    </select>

    <update id="updatePassword" parameterType="com.traffic.practice.dto.UserDTO">
        UPDATE user
        SET password = #{password}
        WHERE user_id = #{userId}
    </update>

</mapper>