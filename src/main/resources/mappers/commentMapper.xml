<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.traffic.practice.mapper.CommentMapper">

    <insert id="register" parameterType="com.traffic.practice.dto.CommentDTO">
        INSERT INTO comment (contents, created_time, user_id, post_id, sub_comment_id)
        VALUES (#{contents}, #{createdTime}, #{userId}, #{postId}, #{subCommentId})
    </insert>

    <update id="updateComment" parameterType="com.traffic.practice.dto.CommentDTO">
        UPDATE comment
        SET contents=#{contents}, created_time=#{createdTime}, post_id=#{postId}, sub_comment_id=#{subCommentId}, updated_time=#{updatedTime}
        WHERE id = #{id}
    </update>

    <delete id="deletePostComment" parameterType="java.lang.Long">
        DELETE FROM comment
        WHERE id = #{commentId}
    </delete>

</mapper>
